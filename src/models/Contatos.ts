class Contatos {
  nome: string
  email: string
  tele: number
  id: number

  constructor(nome: string, email: string, tele: number, id: number) {
    this.nome = nome
    this.email = email
    this.tele = tele
    this.id = id
  }
}

export default Contatos
